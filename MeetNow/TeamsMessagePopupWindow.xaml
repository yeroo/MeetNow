<Window x:Class="MeetNow.TeamsMessagePopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Teams Message"
        Width="420" Height="220"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="DismissButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="6,2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D3D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Background="#2D2D2D" CornerRadius="8" BorderBrush="#444" BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: Urgency badge + dismiss -->
            <DockPanel Grid.Row="0">
                <Border x:Name="UrgencyBadge" CornerRadius="3" Padding="8,3" DockPanel.Dock="Left"
                        Background="#D13438" VerticalAlignment="Center">
                    <TextBlock x:Name="UrgencyText" Text="URGENT" Foreground="White"
                               FontSize="11" FontWeight="Bold"/>
                </Border>
                <TextBlock x:Name="TimeText" Text="" Foreground="#888" FontSize="11"
                           VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button Content="X" Style="{StaticResource DismissButton}"
                        DockPanel.Dock="Right" HorizontalAlignment="Right"
                        Click="DismissClick"/>
            </DockPanel>

            <!-- Sender -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,4">
                <Grid Width="32" Height="32" Margin="0,0,8,0">
                    <Ellipse Fill="#4F52B2"/>
                    <TextBlock x:Name="SenderInitials" Text="AB" Foreground="White"
                               FontSize="13" FontWeight="SemiBold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="SenderName" Text="Sender Name" Foreground="#F0F0F0"
                               FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ChatType" Text="Direct message" Foreground="#888" FontSize="11"/>
                </StackPanel>
            </StackPanel>

            <!-- Message content -->
            <TextBlock Grid.Row="2" x:Name="MessageContent" Text="Message content here..."
                       Foreground="#CCC" FontSize="13" TextWrapping="Wrap"
                       MaxHeight="60" TextTrimming="CharacterEllipsis"
                       Margin="0,6,0,0"/>

            <!-- Urgency reason -->
            <TextBlock Grid.Row="3" x:Name="UrgencyReason" Text=""
                       Foreground="#888" FontSize="11" FontStyle="Italic"
                       Margin="0,8,0,0"/>
        </Grid>
    </Border>
</Window>
